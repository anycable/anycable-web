language: node_js
cache:
  directories:
    - node_modules
    - jspm_packages
script:
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: "./build"
  on:
    branch: master
